// Byte-Bot frontend config.
// Hosted frontend on Vercel must call the FastAPI backend on Render.
// Local note: for pure local testing, temporarily change this to an empty string:
// window.BYTEBOT_BACKEND_URL = "";
window.BYTEBOT_BACKEND_URL = "https://chatbot-ui-m3n0.onrender.com";
